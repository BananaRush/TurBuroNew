<UserControl x:Class="TourAgency.Controls.PdfViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TourAgency.Controls"
             xmlns:moonpdf="clr-namespace:MoonPdfLib;assembly=MoonPdfLib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
        <ScrollViewer
                      VerticalScrollBarVisibility="Visible"
                      PanningMode="VerticalOnly"
                      Name="ScrollViewer">
            <moonpdf:MoonPdfPanel x:Name="PdfPanel"
                                         VerticalAlignment="Top"
                                         Height="{Binding ElementName=ScrollViewer, Path=ActualHeight}"/>

        </ScrollViewer>
    </Grid>
</UserControl>
